<snippet>
	<content>
const N = 100001;
vi adj[N];
int vis[N];
// int dis[N];

void dfs (int u) {
	vis[u] = 1;
	for (auto x : adj[u]) {
		if (vis[x] == 0) {
			dfs(x);
		}
	}
}</content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>zzdfs</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.cpp</scope> -->
</snippet>
